@using Todo.Web.Services

@inject ILoadingService _loading;

@namespace Todo.Web.Components

<MudProgressLinear 
    Color="Color.Primary"
    Style="@(_loading.IsLoading ? "visibility:hidden" : "")"
    Indeterminate="true"
    Class="my-7" />
